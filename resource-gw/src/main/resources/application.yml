server:
  port: 20100
spring:
  application:
    name: resource-gw
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
management:
  # 开放详情端口并允许查看详情
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
logging:
  level:
    org:
      zalando:
        logbook: TRACE
